# 项目开发记忆 (MEMO.md)
最后更新日期: 2026-03-12 (机器人 V2 进化版)

## 1. UI 与 视觉风格 (Visual Identity)
### 极致透明与科技感 (Glassmorphism & Cyberpunk)
- **AI 机器人交互 (AI Smart Ball V2 - 进化版)**:
    - **极致视觉表现**:
        - **全息幻彩银材质**: 引入了 `linear-gradient` (135deg) 配合 `conic-gradient` 虹彩层，模拟随视角流动的光谱效果。
        - **纳米金属纹理**: 叠加了 `brushed-alum` 微细拉丝质感。
        - **凸面玻璃罩 (The Lens)**: 通过 `clipPath` 模拟物理插槽深度，增加 `radial-gradient` 球面反射高光和 `backdrop-blur` 模糊，营造真实玻璃厚度感。
        - **视差深度 (Parallax)**: 内部显示屏层 (`Internal Display`) 与外壳玻璃罩具备不同的位移系数，随鼠标移动产生极强的空间立体感。
    - **多维情感反馈**:
        - **护主模式 (Angry)**: 倒八字眼神 + 亮红核心 + `animate-shake` 高频发抖。
        - **惊讶模式 (Surprised)**: 圆形眼睛 + 圆形惊呼嘴巴，拖拽或点击时即时触发。
        - **思考/助手模式 (AILoading)**: 琥珀色脉冲呼吸灯 + 弯曲微笑嘴型 + 腮红闪烁。
        - **空闲/撒娇 (Idle)**: 粉色柔光 + 待机话术，30秒无操作后自动进入。
    - **全场景拖拽与边界检测**: 保持视口 20px 安全边距，零延迟跟随，松开后平滑回弹。
    - **环境光联动**: 底部增加动态环境反射光 (`Rim Light`) 和地面投影阴影，核心颜色变化时同步渲染周围环境。

- **日历系统增强 (Calendar Core V2)**:
    - **实时天气集成 (WeatherDisplay)**:
        - 接入 Open-Meteo API，实时显示 Rapallo 本地气温与天气图标（晴、云、雨、雪、闪电等）。
        - 具备 `30分钟` 自动刷新机制，图标随天气代码动态切换。
    - **员工效能工具 (Efficiency Tools)**:
        - **空闲标签 (IdleGapLabels)**: 在日历背景中自动标注大于 15 分钟的技师空闲时段。
        - **间隙徽章 (GapBadge)**: 精确显示预约之间的分钟数，辅助老板进行“插空”排单。
        - **状态计时器 (StaffTimer)**: 实时监测员工当前状态（BUSY/IDLE/FREE），并提供动态分钟倒计时。
    - **移动端交互革命**:
        - **iOS 滚动锁定**: 引入 `position: fixed` 与 `top` 偏移量配合的锁定方案，彻底解决 iOS 浏览器在弹窗时的弹性回弹 Bug。
        - **智能时间预设**: 点击日历时根据位置自动判定 AM/PM，减少 50% 的手动切换点击。

- **登录与认证增强 (Auth UI & Logic)**:
    - **游客模式 (Guest Mode)**: 登录页新增入口，进入“我的”页面展示受限 UI。
    - **登录状态持久化 (Persistence)**: 结合 `localStorage` 实现刷新后自动同步身份。
    - **登录失效唤醒 (Session Expiry Modal)**: 全屏高斯模糊提示，保障账户安全。

- **手势导航革命 (Gesture Navigation)**:
    - **全向滑动切换**: `/me` 右滑至 `/explore`，发现页左右滑动切换分类。
    - **手势连线时间选择器**: 废弃数字滚轮，采用“一笔画”连线设定时间，松开即确定。

- **待确认预约提醒 (Pending Alert)**:
    - **视觉冲击**: `status: 'pending'` 预约块红色边框 + `animate-pulse` 持续闪烁。
    - **逻辑**: 优先展示“您有新订单”，直至回拨核实。

## 2. Interaction Features (交互功能)
...
### 预约删除与机器人联动 (Appointment Deletion & AI Reaction)
- **多场景触发**: 在“移动至回收站”、“永久删除”以及“拆分预约”等所有导致预约减少的场景下，均会触发机器人互动。
- **指定话术**: 机器人会即时弹出气泡：“这个客人真讨厌！主人不要生气，我帮你把他吃掉啦！哼！”，并在 4 秒后恢复冷静。
- **手势删除优化**: 针对“向下滑动删除”手势进行了逻辑加固，确保在删除瞬间，机器人的反应优先级高于手势位移清理，让交互反馈百分之百可见。- **背景系统**: 
    - 引入了全屏背景图 `wallhaven-eo68l8.jpg`，并配合 `fixed` 布局和 `z-index: -10` 确保全局覆盖。
    - 使用 `bg-zinc-950/40` 渐变遮罩层，在保持壁纸通透感的同时确保文字内容可读。
- **顶栏 (Header)**:
    - 实现了完全透明化。移除了原有的 `bg-zinc-950/60` 背景和 `backdrop-blur-3xl` 模糊效果。
    - 通过 `drop-shadow` 增强文字和图标在复杂背景下的辨识度。
- **按钮与分类**:
    - 所有分类按钮采用透明毛玻璃质感，背景设为 `bg-white/10` 或 `bg-black/10`。
    - 图标保留了色彩识别特征（如餐饮的 `text-orange-400`），但背景完全通透。
- **底部导航栏 (Bottom Navigation)**:
    - **极致透明**: 最终调整为 `bg-transparent`，完全移除背景色和模糊效果。
    - **视觉增强**: 
        - 选中项添加了 `bg-cyan-400` 的顶部指示光条，并配合 `blur-2xl` 的发光效果。
        - 导航图标使用 `drop-shadow-[0_2px_4px_rgba(0,0,0,0.8)]` 确保在全背景图下的清晰度。
    - **去重修复**: 从 `layout.tsx` 中移除了旧的 `BottomNav` 组件，解决了底部双导航栏重叠的问题。
- **日历视觉优化**:
    - **已结账状态增强**: 为已结账 (COMPLETED) 的预约块添加了对应员工颜色的边框线（如 FANG 为粉红色边框），解决了在半透明背景下辨识度不足的问题。
- **日历布局与间距优化**:
    - **紧凑型布局**: 为了在单屏显示更多预约信息，将日历的小时高度 (`HOUR_HEIGHT`) 从 `10rem` 优化至 **`5rem`**。
    - **自动缩放**: 15 分钟选择块 (`SLOT_HEIGHT`) 同步缩减至 `1.25rem`，确保时间刻度与预约块比例完美对齐。
    - **12小时制回归**: 时间轴切换为 **12小时制** (AM/PM)，更符合常规商店营业习惯。
    - **智能时间预设**: 点击日历创建预约时，系统会根据当前选中的小时自动切换 AM/PM（8-11点默认为 AM，12点及1点以后默认为 PM），大幅减少手动切换操作。

## 2. Interaction Features (交互功能)
### 预约状态机与流程控制 (Status Machine & Workflow)
- **请求式预约模型 (Request-to-Book)**:
    - **逻辑**: 客户发起的预约默认为 `pending` 状态，不自动占用技师排期。
    - **防骚扰机制**: 通过“待确认”状态过滤虚假/恶意预约，老板手动核实（回拨电话）后才转为正式预约。
- **状态流转**:
    - `pending` (待确认) -> `completed` (已结账/手动完成) -> `deleted` (软删除)。
- **实时同步 (Real-time Sync)**:
    - 集成 Supabase `postgres_changes` 频道。当客户提交预约或老板修改状态时，所有在线端（手机、平板）的日历会即时刷新，无需手动下拉。

### 数据库架构升级 (Database Schema)
- **用户档案系统 (Profiles)**:
    - **表结构**: 新增 `public.profiles` 表，独立存储用户的生日 (`birthday`)、会员等级 (`member_level`)、积分 (`points`) 及自定义昵称。
    - **自动化同步**: 实现了 PostgreSQL 触发器 (`handle_new_user`)。每当有新用户通过 OAuth 或邮箱注册，系统会自动在 `profiles` 表中初始化一条关联记录。
- **存量数据迁移**: 编写并执行了 SQL 脚本，将旧版备注中的 `[STATUS:COMPLETED]` 标签自动转化为标准化的 `status` 字段数据。

### 身份验证与安全增强 (Auth & Security)
- **Auth 漏洞修复**: 彻底移除了基于 `localStorage` 的简易登录校验，全面切换为 Supabase Session 强校验。
- **角色路由保护 (RBAC)**:
    - 实现了基于用户元数据 (`user_metadata`) 的权限分发。
    - 商家强制进入 `/merchant/onboarding`，普通用户进入 `/me`，未授权访问自动重定向至登录页。

### 部署信息 (Deployment)
- **部署平台**: Vercel
- **仓库地址**: [https://github.com/FX-ESTETICA/fx-.git](https://github.com/FX-ESTETICA/fx-.git)
- **生产环境 URL**: [https://fx-rapallo.vercel.app](https://fx-rapallo.vercel.app)
- **最近更新**: 2026-03-15 (已完成 Git 提交、推送并触发 Vercel 生产环境部署)。
- **底部导航激活**: 为底部导航栏的“首页”、“发现”、“我的”按键绑定了真实的 Next.js 路由跳转（`/`, `/explore`, `/me`）。
- **发现页滑动切换 (Swipe Navigation)**:
    - **手势支持**: 在 `ExplorePage` 中实现了 `onTouchStart`, `onTouchMove`, `onTouchEnd` 手势监听。
    - **逻辑**: 左右滑动屏幕可快速切换“全部”、“商家”、“景点”、“海边”、“公园”等分类。
    - **自动滚动**: 切换分类时，顶部的分类栏会通过 `scrollIntoView` 自动平滑滚动到当前选中项的中心。
    - **动画反馈**: 引入了 `animate-fade-in` (CSS 关键帧) 实现内容切换时的淡入位移效果。

### 时间选择器手势革命 (Time Picker Gesture Revolution)
- **手势连线选择**: 彻底废弃了传统的数字输入和确定按钮。用户通过手指从左至右“一笔画”连线数字即可设定时间。
- **三列布局**:
    - 第一列（小时十位）：数字顺序调整为 `2, 1, 0`，且 `2` 位于左上角起始位。
    - 第二列（小时个位）：0-9 数字。
    - 第三列（分钟）：00, 15, 30, 45 刻度。
- **松开即确定**: 采用 `onTouchEnd` 监听，手指离开屏幕瞬间自动保存时间并关闭界面，极大提升了平板操作效率。
- **防冲突锁定**: 当滑动起点位于时间选择器的第一列数字时，系统会自动锁定为“时间选择手势”，禁用全局滑屏逻辑。
- **极致简约 UI**: 移除了所有标题、预览文本和提示词，仅保留核心数字阵列。对比度提升至 `white/80` 以确保强光下的清晰度。

### 访问保护与持久化
- **24小时免登录 (Persistent Unlock)**:
    - **原理**: 引入 `localStorage` 存储解锁时间戳 (`calendar_unlock_time`)。
    - **逻辑**: 成功输入密码 `0428` 后记录当前时间。下次访问时，若距离上次解锁不足 24 小时，系统将自动跳过锁定界面，解决了频繁刷新网页需要重登的痛点。

### 弹窗与手势优化 (Modal & Gestures)
- **状态感知窗口路由**:
    - **新预约/未开始**: 自动组合显示“预约信息”+“项目选择”窗口。
    - **进行中 (未过半)**: 自动组合显示“预约信息”+“会员详情”窗口，方便查看顾客历史。
    - **已过半/已完成**: 自动切换为“服务信息”+“结账预览”窗口，实现快速收银。
- **滑动删除视觉修复 (Anti-Shrink)**:
    - **原理**: 引入 `overscroll-y-contain` 禁用浏览器的橡皮筋回弹效果。
    - **逻辑**: 在 `handleTouchMove` 中增加了顶部边界检测，当用户在窗口顶部向下滑动（准备触发删除）时，强制拦截浏览器默认滚动，解决了窗口底部出现的“缩动/抖动”视觉 Bug。
- **侧边栏手势**:
    - 在左侧菜单栏（Sidebar）增加了**向左滑动隐藏**的功能，提升了单手操作体验。

## 3. 核心功能概览 (Legacy Reference)
本项目是一个美甲店收银与预约管理系统，核心组件为 `Calendar.tsx`。
它集成了预约创建、编辑、结账、金额管理、多技师分配以及日历访问保护功能。

## 4. 关键技术实现 (Security & Logic)
### 版本控制与安全增强
- **强制版本管理**: 引入 `APP_VERSION` 常量（当前 `1.0.2`），配合 Supabase `settings` 表进行版本锁死。
- **数据库行级安全 (RLS)**: 
    - 统一在 `supabase/client.ts` 注入 `x-app-version` Header。
    - SQL 策略：`USING (current_setting('request.headers', true)::json->>'x-app-version' = '1.0.2')`。

### 金额管理与持久化 (Perfect Billing Logic)
- **架构**: 引入 `total_amount` 和 `billing_details` (JSONB) 存储。
- **金额优先级**: 手动总金额 > 技师手动金额 > 项目价格修改。
- **离职员工防护**: 通过更换 API Key 或在 RLS 中硬编码版本号封锁访问权限。
- **报表准确性**: `PerformanceReport.tsx` 增加了状态过滤逻辑，仅计算 `status: 'completed'` 的订单，排除了待确认和已删除数据对财务报表的干扰。

## 5. 已修复的重大问题
- **层级穿透**: 修复了日历锁定遮罩层无法屏蔽 Header 和 Grid 交互的问题。
- **重复底栏**: 修复了 `page.tsx` 自定义底栏与 `layout.tsx` 全局底栏冲突的问题。
- **金额计算**: 修复了已结账预约再次打开时金额翻倍和手动改价丢失的逻辑错误。

## 7. 商业价值与核心亮点
### 为什么该代码具备高溢价空间？
- **极致的移动端交互体验**: 
    - **一笔画时间选择器**: 颠覆传统的数字滚轮，专为高频操作设计，极速提升预约效率。
    - **智能窗口感知路由**: 根据预约状态（新预约、进行中、已过半、已完成）自动切换窗口组合，无需人工干预。
    - **全手势支持**: 支持左滑隐藏菜单、左右滑动切换分类、向下滑动删除，操作流畅度媲美原生 App。
- **现代化技术栈与视觉设计**:
    - **Tailwind 4 + 毛玻璃风格**: 紧随前端潮流，提供极具科技感和透明度的 UI 界面。
    - **响应式日历核心**: 自研的日历组件支持自定义小时高度，在小屏幕上也能展示海量信息。
- **完善的后端集成**:
    - **Supabase 无服务器架构**: 低成本、高扩展，自带版本锁死和行级安全策略（RLS），保障数据安全。
- **业务逻辑闭环**:
    - 涵盖了从预约管理、会员系统、项目定价到多技师分成结算的全流程业务逻辑，直接可用于商业落地。

## 8. 授权与版权保护
- **多重安全防护机制**:
    - **版本锁死**: 通过 `APP_VERSION` 常量与后端安全策略深度绑定。非授权版本无法进行数据写入，有效防止代码被恶意篡改后继续使用。
    - **远程授权校验**: 集成了基于 Supabase 的远程授权检测。系统启动时会校验当前版本的合法性，未授权设备或版本将触发锁定状态。
    - **API 防御**: 统一注入自定义请求头，利用数据库层面的策略直接拦截非法请求。

## 9. 后续开发与扩展建议
- **多店支持**: 目前为单店模式，逻辑上已预留多店扩展接口。
- **数据导出**: 建议后续增加财务报表的一键导出（CSV/Excel）功能。
- **图标引用**: 底部导航栏需确保 `lucide-react` 图标已正确导入。
- **保持原子性**: 修改 `Calendar.tsx` 时需注意 `useEffect` 的依赖项，避免无限循环。
